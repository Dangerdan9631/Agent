# Shared Application Contracts

## Purpose

This directory holds small application-level interfaces shared by multiple Atlas domains.

## Conventions

- Contracts describe Atlas concepts rather than third-party library types.
- Implementations belong in the infrastructure layer.

## Contents

- `logging/` defines diagnostic logging abstractions.
- `output/` defines the dedicated user-facing command output boundary.
