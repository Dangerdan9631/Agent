# TypeScript Tools

## Purpose

This directory contains the pure TypeScript model-generation SDK, its command-line host, and npm workspace integration.

## Contents

- `atlas-ts-sdk` analyzes TypeScript workspaces and produces portable Atlas model values.
- `atlas-ts-cli` handles process input and persists generated model documents.
- `atlas-ts-npm` exposes configuration-driven npm lifecycle wrappers.
