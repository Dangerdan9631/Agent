# Atlas TypeScript npm Integration

## Purpose

This package translates npm lifecycle wrapper commands into explicit `atlas-ts` CLI invocations.

## Contents

- `src/` contains the narrow executable wrapper.
