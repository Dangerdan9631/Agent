# Atlas TypeScript Generator Source

This source tree contains the command boundary for TypeScript model generation.
The generator uses the portable Atlas model contract and does not render or
validate architecture diagrams.
