# Ruby Tools

## Purpose

This directory contains the Ruby SDK, CLI, and Rake integration as independently packaged gems in one Bundler workspace.

## Contents

- `atlas-rb-sdk` owns process-independent Ruby source analysis.
- `atlas-rb-cli` owns command parsing and YAML model-file output.
- `atlas-rb-rake` installs native Rake task wiring.
