# Atlas Ruby CLI

## Purpose

This gem owns command parsing, configuration validation, diagnostics, and portable YAML file output through the Ruby SDK.

## Contents

- `exe/` contains the `atlas-rb` executable.
- `lib/` contains CLI application and infrastructure behavior.
- `schemas/` contains packaged JSON Schema contracts.
