# Kotlin SDK Tests

## Purpose

This directory verifies source-model extraction and semantic relationship
behavior independently from CLI and Gradle adapters.

## Conventions

Prefer focused source snippets and deterministic identity assertions. Keep
framework-bound integration behavior in the owning adapter module.
