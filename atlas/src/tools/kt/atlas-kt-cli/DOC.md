# Atlas Kotlin CLI

## Purpose

This application parses Kotlin generation requests, invokes `atlas-kt-sdk`, and persists portable YAML model documents.

## Contents

- `src/main` contains the executable command host and document writer.
- `src/test` contains command-boundary tests.
