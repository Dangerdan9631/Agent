# Kotlin CLI Tests

## Purpose

This directory verifies the Kotlin command boundary, request translation, and
portable YAML output behavior.

## Conventions

Exercise the CLI through public command behavior and use isolated temporary
workspaces for generated artifacts.
