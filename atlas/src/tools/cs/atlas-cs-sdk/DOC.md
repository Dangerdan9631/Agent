# Atlas C# SDK

## Purpose

This package owns Roslyn-backed C# project discovery, semantic extraction, portable model creation, and model linking without CLI or MSBuild-target behavior.

## Contents

- `Configuration/` contains immutable discovery policy values used during analysis.
- `Infrastructure/` contains Roslyn and MSBuild workspace adapters.
- `Model/` contains portable generation inputs and results.
