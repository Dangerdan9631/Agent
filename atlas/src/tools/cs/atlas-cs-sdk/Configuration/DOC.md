# C# SDK Configuration

## Purpose

This directory defines immutable Atlas policy values consumed during C# source
selection and portable model generation.

## Conventions

Configuration values contain no parsing, filesystem, CLI, or MSBuild behavior.

## Contents

- `AtlasConfiguration.cs` defines the validated policy shape used by the SDK.
