{
  "name": "@starcruisestudios/atlas-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "src/atlas-cli",
    "src/atlas",
    "src/tools/atlas-ts",
    "examples/typescript",
    "examples/typescript/packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build:node && npm run build:kotlin && npm run build:examples",
    "build:node": "npm run build --workspace=@starcruisestudios/atlas-cli && npm run build --workspace=@starcruisestudios/atlas && npm run build --workspace=@starcruisestudios/atlas-ts",
    "build:kotlin": "node ./src/tools/atlas-kt-gradle/scripts/RunGradle.mjs build",
    "build:examples": "npm run build:example:typescript && npm run build:example:kotlin",
    "build:example:typescript": "npm run build --workspace=@atlas-example/typescript-workspace",
    "build:example:kotlin": "node ./src/tools/atlas-kt-gradle/scripts/RunGradle.mjs --project examples/kotlin build",
    "test": "npm run test:node && npm run test:kotlin",
    "test:node": "npm run test --workspaces --if-present --include-workspace-root=false",
    "test:kotlin": "node ./src/tools/atlas-kt-gradle/scripts/RunGradle.mjs test",
    "lint": "npm run lint --workspaces --if-present --include-workspace-root=false",
    "typecheck": "npm run typecheck --workspaces --if-present --include-workspace-root=false",
    "format:check": "npm run format:check --workspaces --if-present --include-workspace-root=false",
    "verify": "npm run typecheck && npm run lint && npm run format:check && npm run test && npm run build"
  }
}
