# Application source tree

## Purpose

This source tree contains the executable Kotlin application artifact. Production code is organized under `main/kotlin`.

## Conventions

- Compose external and catalog dependencies at the application boundary.
- Keep executable startup behavior in a dedicated composition root.
- Add a focused README when introducing a new source-area responsibility.

## Contents

- `main/kotlin` contains application production code.
