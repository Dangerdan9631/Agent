# Reading List Library Source

## Purpose

This directory contains reusable reading-list domain behavior with no dependency on the executable package.

## Contents

- `ReadingListItem.ts` normalizes titles and creates stable slugs.
- `ReadingList.ts` creates and retains reading-list items.
