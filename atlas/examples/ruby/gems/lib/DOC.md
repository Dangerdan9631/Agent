# Reading-List Library Gem

## Purpose

This gem validates book titles and creates reading-list items with stable slugs.

## Conventions

The gem is reusable and has no dependency on the executable application gem.

## Contents

- `ReadingListItem` represents normalized output.
- `ReadingList` validates input and creates items.
