# C# Example Catalog Infrastructure

## Purpose

This directory implements application ports with in-memory storage, fixed time,
and source-generated JSON serialization.

## Conventions

- Keep framework APIs behind application-owned interfaces.
- Configure source generation declaratively and map transport values into domain values.

## Contents

- Repository, clock, JSON reader, seed records, and serializer context live here.
