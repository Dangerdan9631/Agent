# C# Application Modules

## Purpose

This directory contains the executable and reusable modules in the C# example.

## Conventions

Dependencies flow from `app` to `lib`, and each module owns its public types.

## Contents

- `app/` contains the command-line delivery module.
- `lib/` contains the reading-list library module.
