# Package/Module Name - Path Relative to Package Source Root

<!--
High level overview of the purpose, conventions, and contents of the directory.

- The overview should be concise and focused on the intent of the code rather
  than implementation details.
- The overview should not be a list of the files in the directory, but rather a
  description of the responsibilities and abstractions contained within.
- Modifying files in the directory should not typically require changes to the
  overview.
-->

## Structure

<!--
Only include this section if relevant.

Include a mermaid diagram of the architectural structure of this directory and
it's relationship to other directories as well as the generalized structure of
the child directories and types within.

- Keep it generalized, avoid including each specific file and type. Reference
  the abstract roles that are played by the files and types in this directory.
- Reference dependencies and dependants at the module level. Do not reference
  outside files directly.
- The arrows should be labeled with 1-5 words that describe the relationship,
  not specific implementation details.
-->

## Conventions

<!--
Only include this section if relevant.

Include H3 subsections that define the conventions used when working in this
directory.

- These should be conventions for this directory directly, and relationships
  between its sub directories and other outside directories.
- Do not reproduce or include conventions for working in the child directories.
- Do not reproduce or include conventions for working in the repo as a whole
  unless they can be augmented or specialized for working in this folder.
- Keep them generalized and avoid referencing specific details of classes or
  implementation.
-->
