# Local install

Self-contained project-local CLI runtime under `.spec-n-roll/cli/`. This area stages bundled toolkit artifacts into initialized projects, validates on-disk layout before dispatcher handoff, and refreshes installs from the global toolkit when the interactive UI requests it.
