# SDK Interactive

Interactive SDK services assemble read-only view data for terminal application screens. They keep file-system parsing and project artifact interpretation in the SDK layer so Ink components can focus on routing, input, and rendering.
