# Workflow Screens

Workflow screens display configured workflow definitions and their step composition. They should treat workflow configuration as read-only unless a later flow explicitly delegates a mutation to an existing workflow command or core operation.

Set list management (triage descriptions, priority, enabled state) lives under Project → Set Lists, not on these workflow definition screens.
