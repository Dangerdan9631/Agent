# Interactive Multi-Spec Fixture

Initialized fixture project for interactive CLI browse and parity tests. It includes two recognized task specs with different lifecycle statuses, project workflow configuration with one enabled agent, and one unrecognized `specs/` directory for warning-path coverage.
