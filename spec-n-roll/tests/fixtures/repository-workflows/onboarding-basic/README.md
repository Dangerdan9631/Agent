# Onboarding Basic Fixture

Initialized fixture project for repository onboarding integration and contract tests. The project has greeting code, tests, and documentation but no `living-specs/` directory.
