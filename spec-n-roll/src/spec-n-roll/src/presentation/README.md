# Dispatcher presentation layer - src/spec-n-roll/src/presentation

This directory contains command-line entry adapters for dispatcher execution. It translates user-facing command invocation into application requests.

## Conventions

### Presentation adapters

Keep argument parsing and command framework details in this layer. Delegate decisions to application classes after inputs are shaped.
