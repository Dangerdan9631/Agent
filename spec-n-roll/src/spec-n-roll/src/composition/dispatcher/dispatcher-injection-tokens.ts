/**
 * Identifies the dispatcher environment registration in the dependency container.
 */
export const dispatcherEnvironmentToken = 'spec-n-roll.dispatcherEnvironment';

/**
 * Identifies the runtime process executor registration in the dependency container.
 */
export const runtimeProcessExecutorToken = 'spec-n-roll.runtimeProcessExecutor';

/**
 * Identifies the dispatcher logger registration in the dependency container.
 */
export const dispatcherLoggerToken = 'spec-n-roll.dispatcherLogger';
