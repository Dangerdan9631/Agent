# Dispatcher metadata domain - src/spec-n-roll/src/infrastructure/metadata

This directory contains adapters for reading dispatcher installation metadata. It turns filesystem metadata into shared API contracts.

## Conventions

### Metadata access

Keep file layout and marker parsing details here. Expose resolved metadata through focused classes rather than raw filesystem operations.
