# spec-n-roll

This package owns the public `spec-n-roll` and `snr` dispatcher executables. It parses dispatcher-only routing flags, resolves project context, and delegates execution to either a project-local CLI launcher or the global runtime package.
