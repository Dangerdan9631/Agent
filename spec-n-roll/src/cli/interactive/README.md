# Interactive CLI Entry

The interactive entry directory isolates bare-invocation launch behavior from non-interactive Commander routing. It resolves the project context, renders the Ink app, and owns terminal lifecycle details without duplicating command implementation logic.
