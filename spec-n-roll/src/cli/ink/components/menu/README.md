# Menu Components

Shared helpers for constructing consistent selectable menu rows across interactive screens.

This directory provides small builders such as the Back row helper so list routes can append navigation exits without duplicating label formatting or stable row ids.
