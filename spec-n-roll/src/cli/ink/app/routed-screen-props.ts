/**
 * Props shared by routed screens that receive the app scaffolding route content row budget.
 */
export interface RoutedScreenProps {
  /**
   * Rows allocated by app scaffolding to the active route content slot. Must be zero or greater.
   */
  routeContentRows: number;
}
