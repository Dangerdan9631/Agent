# Ink App

The app directory owns the interactive shell that will render when the CLI is launched without a subcommand. It contains session state, route definitions, navigation helpers, and top-level layout behavior that coordinates screens without performing business mutations.
