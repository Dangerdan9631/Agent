# Workflow Screens

Workflow screens display configured workflow variants and their step composition. They should treat workflow configuration as read-only unless a later flow explicitly delegates a mutation to an existing workflow command or core operation.
