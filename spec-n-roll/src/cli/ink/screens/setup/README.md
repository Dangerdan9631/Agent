# Setup Screens

Setup screens host project initialization, toolkit version display, and maintenance workflows. They should compose existing init and update prompts so setup behavior remains consistent with non-interactive command entry points.
