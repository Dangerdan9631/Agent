# MCP presentation layer - src/spec-n-roll-mcp/src/presentation

This directory contains entry adapters for MCP commands. It translates process invocation into composed MCP behavior.

## Conventions

### Presentation adapters

Keep command-line startup concerns here. Delegate command behavior to composed application services.
