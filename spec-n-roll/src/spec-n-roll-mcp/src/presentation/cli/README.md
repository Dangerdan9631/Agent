# MCP CLI domain - src/spec-n-roll-mcp/src/presentation/cli

This directory contains the MCP command-line adapter. It owns CLI startup shape for the MCP executable.

## Conventions

### CLI shape

Keep CLI argument handling focused and narrow. Do not add command execution implementation details to the CLI adapter.
