# MCP commands domain - src/spec-n-roll-mcp/src/application/commands

This directory contains command execution contracts for MCP workflows. It defines the application boundary implemented by command adapters.

## Conventions

### Command boundary

Keep contracts small and substitutable. Do not add process or protocol framework details to command interfaces.
