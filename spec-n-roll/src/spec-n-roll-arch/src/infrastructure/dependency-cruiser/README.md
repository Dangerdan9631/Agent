# Dependency cruiser infrastructure domain - src/spec-n-roll-arch/src/infrastructure/dependency-cruiser

This directory contains dependency-cruiser execution adapters. It isolates command execution and tool output collection.

## Conventions

### Tool access

Keep dependency-cruiser invocation details here. Convert tool output into application graph models outside the process runner.
