# Workspace infrastructure domain - src/spec-n-roll-arch/src/infrastructure/workspace

This directory contains filesystem adapters for discovering workspace package information. It turns package files and workspace roots into architecture models.

## Conventions

### Workspace reads

Keep filesystem and package-json reads here. Package policy and artifact generation should remain in application domains.
