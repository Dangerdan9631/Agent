# Architecture packages domain - src/spec-n-roll-arch/src/application/packages

This directory contains workspace package models and package policy behavior. It decides which packages participate in architecture workflows.

## Conventions

### Package policy

Keep package selection and policy rules explicit in this domain. Workspace filesystem reads should be hidden behind infrastructure adapters.
