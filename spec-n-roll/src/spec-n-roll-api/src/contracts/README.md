# API contract layer - src/spec-n-roll-api/src/contracts

This directory contains serialized and cross-package contracts shared by executable packages. Domains group contracts by the vocabulary they describe.

## Conventions

### Contract focus

Keep these files representation-focused and free of process, filesystem, or parsing behavior. Add a domain directory when a new shared vocabulary appears.
