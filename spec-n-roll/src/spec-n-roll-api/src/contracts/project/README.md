# Project contracts domain - src/spec-n-roll-api/src/contracts/project

This directory contains shared project-root and path-resolution contracts. These types describe resolved project context without performing resolution.

## Conventions

### Project vocabulary

Keep path values explicit about form and constraints. Do not add filesystem access or discovery behavior to this domain.
