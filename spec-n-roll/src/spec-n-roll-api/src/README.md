# API source

This source directory contains boundary contracts that must remain stable across globally and locally installed packages. The exported constants and types describe project discovery paths, dispatcher install-source metadata, and the JSON payload sent from dispatcher to runtime.
