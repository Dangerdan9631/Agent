# Templates

Toolkit-owned step output templates for task spec artifacts. This layer holds the canonical `spec.md`, `plan.md`, and `tasks.md` templates copied into task spec directories before agent prose edits begin.

Templates include inline fill instructions and tier-aware expectations enforced by the core instantiation layer.
