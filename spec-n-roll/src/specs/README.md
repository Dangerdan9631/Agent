# Specs

Workflow step handlers for specification-driven task creation and advancement. This layer orchestrates triage, interviews, planning, task breakdown, and implementation entry without owning deterministic file mutations.

Step handlers invoke the core library for template instantiation, lifecycle transitions, and workflow state updates while agents edit prose bodies directly.
