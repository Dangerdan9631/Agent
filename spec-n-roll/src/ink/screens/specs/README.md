# Task Spec Screens

Task spec screens present lifecycle state, workflow progress, artifacts, warnings, and task-specific actions for specs found under `specs/`. They should preserve the read-only boundary for browse views and route mutations through existing task, workflow, and step operations.
