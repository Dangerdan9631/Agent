# SDK identity domain - src/spec-n-roll-sdk/src/application/identity

This directory contains SDK behavior for package identity decisions. It centralizes package names used by current executable stubs.

## Conventions

### Identity behavior

Keep identity values and related behavior focused here. Avoid adding transport or command-line responsibilities to SDK identity classes.
