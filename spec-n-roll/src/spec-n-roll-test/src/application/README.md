# Test support application layer - src/spec-n-roll-test/src/application

This directory contains reusable test-support behavior independent of process adapters. Domains model command helpers and package expectations used across tests.

## Conventions

### Application helpers

Keep test helper behavior focused and reusable across packages. Process execution details belong in infrastructure.
