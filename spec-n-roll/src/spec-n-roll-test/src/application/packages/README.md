# Test support packages domain - src/spec-n-roll-test/src/application/packages

This directory contains shared package expectations for workspace tests. It centralizes package-name data used by test suites.

## Conventions

### Package expectations

Keep package expectation values focused and documented. Avoid adding package discovery or process behavior here.
