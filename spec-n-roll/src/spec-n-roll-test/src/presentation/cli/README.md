# Test support CLI domain - src/spec-n-roll-test/src/presentation/cli

This directory contains the test-support command-line adapter and executable entry implementation. It owns CLI startup shape for test support.

## Conventions

### CLI shape

Keep CLI argument handling focused and narrow. Do not add command runner implementation details to CLI classes.
