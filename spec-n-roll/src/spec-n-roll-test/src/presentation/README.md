# Test support presentation layer - src/spec-n-roll-test/src/presentation

This directory contains entry adapters for test-support commands. It translates process invocation into composed test-support behavior.

## Conventions

### Presentation adapters

Keep command-line startup concerns here. Delegate reusable helper behavior to application and composition classes.
