# Runtime composition layer - src/spec-n-roll-runtime/src/composition

This directory contains dependency wiring for runtime execution. It connects runtime application behavior to concrete process adapters.

## Conventions

### Composition only

Keep assembly and dependency construction here. Do not add invocation parsing or output behavior to composition classes.
