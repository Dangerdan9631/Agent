# Filesystem infrastructure

This directory contains Node.js filesystem adapters used by runtime application boundaries, including project initialization.

## Conventions

Keep raw filesystem operations and platform-specific launcher details here. Application orchestration depends only on narrow filesystem-facing interfaces.
