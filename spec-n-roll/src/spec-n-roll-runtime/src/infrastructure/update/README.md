# Project update infrastructure

This directory contains Node.js implementations for project framework updates, ownership checks, and configuration migration. These adapters keep direct filesystem access outside runtime application orchestration.
