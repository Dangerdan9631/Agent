# Project update application behavior

This directory contains runtime application boundaries for updating local project framework files and migrating project configuration. The infrastructure layer supplies the filesystem-backed implementations.
