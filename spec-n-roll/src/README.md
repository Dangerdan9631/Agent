# New workspace packages

This directory contains the new multi-package implementation of spec-n-roll. Each package owns one runtime, library, architecture, or test responsibility so package boundaries stay explicit as the new version grows.
