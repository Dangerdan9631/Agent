{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec-n-roll.local/contracts/install-manifest.schema.json",
  "title": "Spec-n-Roll CLI Install Manifest",
  "description": "Metadata for a project-local self-contained CLI install at .spec-n-roll/cli/install.json.",
  "type": "object",
  "required": ["toolkitVersion", "layoutVersion", "installedAt"],
  "additionalProperties": false,
  "properties": {
    "toolkitVersion": {
      "type": "string",
      "minLength": 1,
      "description": "Semver of the bundled toolkit runtime pinned for this project."
    },
    "layoutVersion": {
      "type": "integer",
      "minimum": 1,
      "description": "Local install layout generation. Value 1 denotes self-contained bundle layout."
    },
    "installedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when binaries were last successfully installed or refreshed."
    },
    "installSource": {
      "type": "string",
      "enum": ["global", "registry", "linked-source"],
      "description": "Optional provenance hint for display; does not affect execution resolution."
    }
  }
}
