# Feature Specification: Spec-n-Roll Toolkit

**Feature Branch**: `001-spec-n-roll-toolkit`

**Created**: 2026-06-09

**Status**: Complete

**Input**: User description: "spec-n-roll is a toolkit is an AI development workflow for spec driven development..."

---

## Clarifications

### Session 2026-06-09

- Q: Should living spec Gherkin scenarios be tagged with the originating task spec ID? → A: Yes. When a task spec creates or updates living spec scenarios, those scenarios MUST be tagged with the task spec ID (e.g., `@spec-n-roll-001`). This enables running only the Cucumber tests relevant to a specific task.
- Q: What naming convention should be used for agent workflow commands? → A: All commands MUST use the `spec-n-` prefix (e.g., `/spec-n-specify`, `/spec-n-plan`, `/spec-n-implement`). A `/spec-n-roll` meta-command MUST also exist that advances the workflow to the next appropriate step without the developer needing to know the current state or selected workflow tier.
- Q: Should individual workflow definitions be user-configurable? → A: Yes. Workflows MUST be definable in configuration files in the user's project setup. A default workflow MUST be provided on init. The extension model MUST support multiple named workflow definitions, step reuse across workflows, and triage/selector steps that choose between workflows at runtime.
- Q: How should `/spec-n-roll` determine the current workflow position? → A: An explicit state file is written to the task spec directory after each step completes. If the state file is absent or unreadable, artifact detection (presence/absence of spec.md, plan.md, tasks.md, etc.) is used as a self-healing fallback.
- Q: What is the granularity of the task ID tag applied to Gherkin scenarios? → A: One tag per workflow run using the task spec directory ID (e.g., `@spec-n-roll-001`). All scenarios created or modified in a single workflow run share the same tag.
- Q: When a workflow step is interrupted mid-execution and `/spec-n-roll` is next invoked, what should happen? → A: Restart the interrupted step from the beginning. If partial artifacts are detected, warn the developer and ask for a single confirmation before proceeding — one confirmation covers all artifacts for that step, not one confirmation per artifact.
- Q: When a later workflow run modifies a Gherkin scenario already tagged from a prior run, how should tags accumulate? → A: Additive — all task tags are retained. A scenario modified by tasks 001 and 002 carries both `@spec-n-roll-001` and `@spec-n-roll-002`. Tags are never removed from a scenario.
- Q: Should documentation be a first-class deliverable of the toolkit? → A: Yes. The toolkit MUST ship comprehensive documentation covering the overall workflow, the extension/plugin process, multi-agent setup, platform configuration, CLI usage, and the update/migration model. Documentation must be treated as a first-class feature, not an afterthought.
- Q: Should a CLI application be provided for toolkit setup, update, and configuration? → A: Yes. A TypeScript CLI application MUST be provided that enables developers to set up a new toolkit installation, update an existing installation, and modify configuration. This is the primary non-agent interaction surface for the toolkit.
- Q: Should the toolkit be versioned to support safe updates? → A: Yes. The toolkit MUST be versioned. The CLI MUST support checking for updates and applying them. Toolkit files MUST be classified as either toolkit-owned (overwritten on update) or user-owned (preserved on update). These two categories MUST NOT overlap.
- Q: How should config schema changes be handled across toolkit versions? → A: Config files MUST use a versioned schema designed for backward compatibility. Projects using older config schemas MUST continue to work after a toolkit update. Breaking changes to config schemas require a migration path that the CLI can assist with.
- Q: What is the precise backward compatibility mechanism for config schemas? → A: Breaking schema changes are permitted. Schema migration happens at CLI update time — not JIT at runtime. The CLI uses an additive-only "read schema" (tolerant reader) to parse any prior config version, migrates it to the current schema, and writes the migrated config back to disk as part of the update. At runtime (outside of an update), the toolkit may assume all configs are already at the current schema version. Schema migration logic lives in the CLI update path only.
- Q: Can extensions declare toolkit version compatibility, and what happens on a mismatch? → A: Extensions declare the specific toolkit version they were designed for in their manifest. The toolkit maintains a compatibility declaration listing toolkit versions it is known to be incompatible with for a given extension. Version mismatches surface as warnings at upgrade time — they are never shown at runtime and never block execution.
- Q: What is the guiding principle for classifying a file as toolkit-owned vs user-owned? → A: Intent-based, made clear through directory location — toolkit-owned files live in designated toolkit directories; user-owned files live in separate, designated user directories. The directory location communicates and enforces the ownership intent. A file's location determines its ownership class.
- Q: When a toolkit update would overwrite a toolkit-owned file that the user has locally modified, what should happen? → A: Overwrite with backup — the CLI saves a `.bak` copy of any locally modified toolkit-owned file before overwriting it, and reports each conflict in the update summary. The developer can recover their changes from the backup. The toolkit-owned file is always updated to the new version.
- Q: How is the TypeScript CLI distributed, installed, and versioned? → A: ~~Global CLI delegates in-process to local copy~~ **Superseded (2026-06-10)**: Globally installed artifact is a lightweight dispatcher only; see Session 2026-06-10 (CLI Dispatcher).
- Q: How is a task spec "marked complete" and locked to enforce immutability? → A: The toolkit automatically marks a task spec complete when it executes the final workflow step. After completion, the user may request additional actions (e.g., re-run tests, re-analyze) without the spec being locked. Locking (full immutability) is triggered when a NEW workflow run begins any step beyond `/spec-n-specify`. `/spec-n-specify` itself never locks a prior task spec, allowing multiple specs to coexist before any implementation begins.
- Q: Where does the toolkit's documentation live and how is it accessed? → A: Markdown files in the toolkit repository root `docs/` directory only — no hosted site. Documentation is not installed into user projects.
- Q: How should documentation files be classified under the file ownership model? → A: Documentation files live only in the toolkit repository root `docs/` and are not installed into user projects — they are entirely excluded from the toolkit-owned vs. user-owned classification.
- Q: What depth and structure is required for extension/plugin documentation? → A: Quick-start guide + reference (manifest schema, hook events, step interfaces) + at least one fully worked extension example. This structure applies to the extension/plugin documentation specifically and must be at least as thorough as the built-in workflow documentation.
- Q: Do extension interfaces carry formal stability or deprecation guarantees? → A: The toolkit version as a whole follows semver. Breaking changes to any extension interface (step contracts, hook event shapes, manifest schema) require a toolkit major version bump. Extension authors rely on the toolkit's semver guarantee — interfaces are not individually versioned and no per-interface deprecation cycle is required.
- Q: When a developer declines the interrupted-step confirmation, can they inspect partial artifacts before deciding? → A: Yes. The confirmation prompt MUST offer three choices: restart (overwrite all partial artifacts), cancel (leave partial artifacts in place for inspection), or force-clean (delete all partial artifacts and restart). Cancelling leaves the workflow in a paused state that can be resumed via `/spec-n-roll`.

### Session 2026-06-09 (Design Interview)

- Q: How should task spec directory IDs be assigned? → A: Hybrid model. The toolkit auto-assigns a unique, sequential, zero-padded numeric ID (e.g., `001`). The developer may optionally provide a descriptive slug; if omitted, the toolkit derives a kebab-case slug from the feature description (truncated as needed). Slugs are not required to be unique; only the numeric ID must be unique. Directory format: `specs/{numeric-id}-{slug}/`.
- Q: Where and how should living specification Gherkin files be organized? → A: A dedicated user-owned directory (e.g., `living-specs/`) containing one `.feature` file per application domain or capability area. The toolkit routes updates to the appropriate file.
- Q: How should deprecated living spec scenarios be handled? → A: Removed from living spec files entirely. Version control history is the sole archival record — no in-repo archive directory or archive tags.
- Q: When the workflow state file conflicts with artifacts on disk, which source wins? → A: The state file wins when present and parseable. The toolkit warns about the artifact mismatch and requires a single confirmation before proceeding.
- Q: What happens when the feature description is empty or too ambiguous to triage? → A: Present all available workflow tiers and require the developer to pick one manually, skipping triage.
- Q: How does initialization handle agents that are not installed? → A: Agent selection is purely user input during initialization. The toolkit generates configuration for selected agents with no verification that any agent is installed.
- Q: How are conflicting living spec scenarios detected? → A: No dedicated conflict-detection step. The toolkit attempts to update all relevant living specs as new functionality is defined. Contradictions are surfaced by automated Cucumber test runs.
- Q: What happens when Gherkin steps have no matching step definitions? → A: Generate stub step definitions in the failing test suite, clearly marked as stubs requiring implementation.
- Q: What post-completion actions are allowed on Complete task specs? → A: Unrestricted — any workflow command may run against a Complete spec until locking occurs. When `/spec-n-clarify` adds new un-implemented requirements, they are appended to the spec and the task spec reverts from Complete to Active.
- Q: Where should task spec directories live? → A: A dedicated user-owned `specs/` directory with one `{numeric-id}-{slug}/` subdirectory per workflow run.
- Q: Where do generated Cucumber tests live relative to living specs? → A: Living specs are the Cucumber Gherkin `.feature` files in `living-specs/`; Cucumber runs against them directly. Step definitions and executable test code live in the project's standard test location — no duplicate feature files.
- Q: What happens when no configured script runtime is available on the current platform? → A: Fail with a clear error identifying the missing shell runtime and remediation steps.
- Q: At which workflow step(s) should living specs be updated? → A: Living spec updates occur at implementation entry. The tasks step MUST list updating living specs as its first task(s), executed before test generation or production code.
- Q: Can multiple task specs be in implementation simultaneously? → A: No. Only one Active task spec may be in the implement phase at a time; others must remain in pre-implement stages.
- Q: What happens when an extension fails at runtime after an upgrade warning? → A: Fail the current workflow step with a clear error citing the extension and prior upgrade warning, with remediation guidance. Warnings are not retroactively upgraded to block startup.
- Q: How should `/spec-n-roll` determine which task spec to advance when multiple are Active? → A: A versioned project-level metadata file identifies the current task. `/spec-n-roll` uses this file at implement time; pre-implement routing is governed separately (see next item).
- Q: When is the current-task metadata file updated? → A: Validated and set when the implement step begins, establishing which task spec is authoritative for implementation.
- Q: How are pre-implement workflow commands scoped when multiple Active task specs exist? → A: ~~Require an explicit task ID~~ **Superseded (2026-06-10)**: interactive numbered-list prompt when no task is identified.
- Q: What is the relationship between `/spec-n-specify` and `/spec-n-clarify`? → A: `/spec-n-specify` creates a new task spec and includes the initial iterative interview. `/spec-n-clarify` is a follow-up step for additional clarifications on an existing spec, with its own iterative interview process for those follow-ups.
- Q: What should `/spec-n-analyze` do? → A: Cross-artifact consistency and quality analysis across the current task spec's spec, plan, tasks, and optionally living specs — producing a non-destructive report of gaps, contradictions, and checklist failures.
- Q: When a Complete spec reverts to Active via clarify, what happens to other Complete specs? → A: Other Complete specs remain Complete (unlocked) until the reverted spec begins a non-specify step or a different task spec begins a non-specify step.

### Session 2026-06-10 (Design Interview — Plan & Contracts)

- Q: What is the project directory layout (toolkit-owned vs user-owned)? → A: Split roots — toolkit-owned `.spec-n-roll/` (CLI, scripts) and `.agents/` (generated skills); user-owned `.spec-n-roll/config/` (workflow config, extension registry refs); user-owned `specs/` and `living-specs/` at project root.
- Q: What is the canonical `taskSpecId` format and Gherkin tag convention? → A: `taskSpecId` is numeric only (e.g., `001`); slug is used in directory paths only for human readability. Gherkin tags use `@spec-n-roll-{numeric-id}` (e.g., `@spec-n-roll-001`).
- Q: Where do project metadata and workflow state files live? → A: Project metadata at `.spec-n-roll/config/project-metadata.json`; workflow state at `specs/{id}-{slug}/workflow-state.json`.
- Q: How does living spec file routing work? → A: Agent automatically selects or creates the target domain `.feature` file; planned living-spec creates/updates are documented in `plan.md` and `tasks.md` for developer review before implementation applies file changes.
- Q: What are the default workflow tiers? → A: `specify` is always the tier entry point; `implement` is always the exit. Three default tiers: **papercut** (`specify → implement`), **quick** (`specify → tasks → implement`), **full** (`specify → plan → tasks → implement`).
- Q: Where do `clarify` and `analyze` fit? → A: On-demand steps between tier steps — not in default tiers; manually invokable between any two tier steps; `/spec-n-roll` skips them unless explicitly run.
- Q: How is task selection handled when multiple Active specs exist? → A: Interactive prompt — if multiple Active specs exist and no task is identified, the agent presents a numbered list for the developer to choose (no silent default).
- Q: How does built-in triage select among tiers? → A: Heuristic checklist (e.g., single-file/fix/copy → papercut; new behavior without architecture change → quick; cross-cutting/new subsystem → full); agent presents matched tier with rationale; developer confirms or overrides.
- Q: What are the workflow config and extension paths? → A: Workflow config at `.spec-n-roll/config/workflow.config.json`; user extension manifests at `.spec-n-roll/config/extensions/{id}/manifest.json`.
- Q: Where is the local CLI installed? → A: ~~`.spec-n-roll/cli/bin/spec-n-roll` only~~ **Superseded (2026-06-10)**: Full CLI at `.spec-n-roll/cli/bin/spec-n-roll`; MCP server at `.spec-n-roll/cli/bin/spec-n-roll-mcp` (each with `.cmd` on Windows). See Session 2026-06-10 (CLI Dispatcher).
- Q: What extension hook events are supported? → A: ~~Fixed enum including `before_clarify`/`after_clarify` and `before_analyze`/`after_analyze`~~ **Superseded (2026-06-10)**: Hook events follow `before_{stepId}` / `after_{stepId}` for any registered step ID; `/spec-n-roll` fires the underlying step's hooks.
- Q: Where are extension compatibility declarations stored? → A: Toolkit-bundled at `.spec-n-roll/compatibility.json`, refreshed on every toolkit update.
- Q: How do workflow state `status` and task spec lifecycle relate? → A: Independent concerns aligned on completion — workflow state tracks operational progress (`paused` = interrupted mid-step); task spec tracks lifecycle (`Locked` = immutability); workflow `complete` and task spec `Complete` both occur when `implement` finishes.
- Q: When does triage run relative to `specify`? → A: Meta-flow: `triage-selector → tier variant → specify → … → implement`. Feature description is captured first; triage selects tier; then `specify` runs as that tier's first step.
- Q: How is `.agents/` structured? → A: Skills stay in `.agents/` (agents read from `.agents/skills/`). Canonical agent rules live in `.spec-n-roll/AGENTS.md`; per-agent native files (`AGENTS.md`, `CLAUDE.md`, Cursor rules, etc.) are thin pointers to that file.
- Q: Which agents does v1 support? → A: Extension-only agents — no hard-coded registry. Cursor, claude-code, copilot, and codex ship as out-of-the-box extensions.
- Q: Where do out-of-box agent extensions live? → A: Toolkit-owned at `.spec-n-roll/bundled-extensions/{id}/`; `workflow.config.json` references them by default; user extensions under `config/extensions/`.
- Q: How does `/spec-n-roll` handle multiple Active specs? → A: Same numbered-list interactive prompt as other commands when no task is identified.
- Q: How is the initial feature description captured? → A: `/spec-n-specify <description>` is primary (description → specify with embedded triage → interview). `/spec-n-roll` detects new-spec intent and routes to `/spec-n-specify`.
- Q: How does `/spec-n-roll` detect new-spec vs continue intent? → A: Description argument → new spec via `/spec-n-specify`; no description + zero Active specs → prompt for description; no description + Active specs → task-selection prompt then advance; ambiguous state → "new or continue?" prompt.

### Session 2026-06-10 (Design Interview — Research & Contracts)

- Q: Where does triage run relative to `specify`? → A: ~~Meta-flow: `triage-selector → tier variant → specify`~~ **Superseded**: Triage is embedded in the `/spec-n-specify` step — `specify` is always the first step of every workflow tier. Triage runs at the start of `specify` (before the interview) and selects which workflow tier variant applies for subsequent steps.
- Q: How are tier variants modeled in `workflow.config.json`? → A: Each tier variant (papercut, quick, full) explicitly lists a shared `specify` step reference as step 1, followed by its tier-specific tail (`implement`; `tasks → implement`; `plan → tasks → implement`). Steps are reusable across variants without duplication.
- Q: Where is task spec lifecycle status (`Active`/`Complete`/`Locked`) persisted? → A: In `spec.md` YAML frontmatter (`status: Active|Complete|Locked`). Operational workflow progress (`active`/`paused`/`complete`) remains in `workflow-state.json` as a separate concern.
- Q: What is the machine-readable `taskSpecId` format in JSON contracts? → A: ~~Numeric only; slug directory-only~~ **Superseded**: JSON files carry numeric `taskSpecId` (e.g., `001`) plus a **required** `slug` field (e.g., `my-feature`). Gherkin tags remain numeric-only (`@spec-n-roll-001`). Directory path: `specs/{numeric-id}-{slug}/`.
- Q: How are tier-skipped artifacts handled (e.g., `plan.md` on papercut)? → A: Omitted entirely — no empty stubs. Artifact detection and `/spec-n-roll` respect the selected tier's step list from `workflow.config.json`.
- Q: What does `defaultWorkflowId` mean in `workflow.config.json`? → A: Pre-selected default tier in the manual/override tier picker only (e.g., when the feature description is ambiguous). It does not auto-run a workflow before `specify`; normal heuristic triage within `specify` proposes its own match unless the developer overrides.
- Q: How are extension `entrypoint` values executed? → A: In-process Node module — `entrypoint` is a project-relative path to a JS/TS module exporting a standard handler; the workflow engine `import()`s and invokes it.
- Q: How are sequential numeric task spec IDs assigned? → A: `project-metadata.json` maintains a `nextTaskSpecId` counter, incremented atomically when a new task spec directory is created.
- Q: What is the living spec domain file naming convention? → A: `living-specs/{kebab-case-domain}.feature` (e.g., `living-specs/user-authentication.feature`). The agent infers domain semantically from the feature description; planned targets are documented in `plan.md`/`tasks.md` before implementation writes.
- Q: What defines "partial artifacts" for interrupted-step recovery? → A: Built-in step output manifest — each step ID maps to expected output files (e.g., `specify` → `spec.md`; `plan` → `plan.md`). Partial = any expected file for the current incomplete step exists while `workflow-state.json` shows that step not yet completed. One prompt covers all partial files for that step.

### Session 2026-06-10 (Extension Schema)

- Q: Should extension step `phase` be a fixed enum in the manifest schema? → A: No. The manifest schema MUST NOT close the set of step identifiers. Step IDs are open kebab-case strings declared in `workflow.config.json` and extension manifests; workflows may reference built-in steps, on-demand steps, and custom extension-defined steps. The manifest field is `stepId` (replacing `phase`) and references the workflow step slot this extension replaces or augments.
- Q: Should extension hook `event` values be a fixed enum? → A: No. Hook events follow the pattern `before_{stepId}` and `after_{stepId}` where `{stepId}` is any registered workflow step ID (built-in or extension-defined). `/spec-n-roll` has no dedicated hooks — it fires the underlying step's hooks.
- Q: Should the extension manifest field remain `phase` or be renamed? → A: Rename to `stepId` — a kebab-case string referencing any registered workflow step (built-in, on-demand, or extension-defined).
- Q: Should `before_update`/`after_update` CLI lifecycle hooks be supported? → A: No. Update-time extension integration is out of scope for hook events; extensions integrate at workflow step boundaries only (`before_{stepId}`/`after_{stepId}`). Toolkit upgrade behavior uses CLI commands and documented extension mechanisms instead.
- Q: How should hooks referencing an unknown `stepId` be handled? → A: Warn at manifest load/registration when the parsed `{stepId}` is not in the merged step registry; skip dispatch for that hook. Non-blocking — workflow execution continues.

### Session 2026-06-10 (Deterministic Operations)

- Q: How should the MCP server be deployed relative to the CLI? → A: Project-local MCP server bundled with the toolkit and registered in agent config at `init` (stdio transport, version-locked to the local CLI). The MCP server and CLI expose different interfaces to the same local core library — all deterministic mutations share one implementation.
- Q: Which file changes must agents perform only via MCP/CLI (not direct edits)? → A: Machine-readable state only — `workflow-state.json`, `spec.md` YAML frontmatter (lifecycle status), `project-metadata.json`, and task completion checkboxes in `tasks.md`. Agents MAY directly edit prose bodies in `spec.md`, `plan.md`, and `tasks.md`.
- Q: How should step output templates be applied? → A: MCP/CLI commands instantiate templates by copying template files into the task spec directory; frontmatter values are passed as command arguments when needed. The agent then reads the instantiated file and edits prose content directly. Frontmatter changes MUST go through MCP/CLI — not direct agent edits. Templates contain inline instructions for how to fill out each section; skills document the instantiate command and how to edit content afterward — not the template structure itself.
- Q: Should living spec `.feature` files use MCP/CLI template instantiation? → A: No. Living spec files under `living-specs/` remain fully agent-managed — no MCP/CLI involvement for living spec creates or updates.
- Q: How should the CLI choose interactive vs non-interactive mode? → A: `spec-n-roll` with no arguments spawns the interactive Ink terminal; subcommands are available inside that session. Any invocation with commands/arguments (e.g., `spec-n-roll <subcommand> [args]`) is implicitly non-interactive — runs synchronously and exits.

### Session 2026-06-10 (CLI Dispatcher)

- Q: How should global/local CLI dispatching be structured? → A: The globally installed artifact is a lightweight dispatcher containing only local-vs-global resolution logic. Argument parsing, version output, subcommands, Ink UI, and core library live in the full CLI binary behind the dispatcher; MCP is a separate local binary sharing the core library. When dispatching to a local instance, the global full CLI code MUST NOT be loaded — the dispatcher exec's the local binary as a separate process. MCP invocation MUST always target the project-local MCP binary (never the global dispatcher).
- Q: What should `-v` / `--version` report? → A: ~~Dispatcher intercepts and prints both versions~~ **Refined (2026-06-10)**: Dispatcher forwards `-v`/`--version` unchanged to the resolved full CLI binary. The full CLI binary prints the combined report: dispatcher version, executed binary version, `local`/`global` target, and absolute local binary path when applicable.
- Q: How should the dispatcher locate the project-local full CLI binary? → A: Walk up parent directories from `cwd` to find the nearest `.spec-n-roll/cli/bin/spec-n-roll`.
- Q: What is the MCP server entrypoint binary? → A: Separate local binary at `.spec-n-roll/cli/bin/spec-n-roll-mcp` (with `spec-n-roll-mcp.cmd` on Windows), registered in agent MCP config at `init` — not a CLI subcommand and not the global dispatcher.
- Q: How does the dispatcher locate the global full CLI when no local binary exists? → A: Global full CLI binary is co-bundled in the npm package; the dispatcher resolves it relative to its own install path when walk-up finds no `.spec-n-roll/cli/bin/spec-n-roll`.
- Q: Who handles `-v` / `--version` output? → A: Dispatcher forwards `-v`/`--version` to the resolved full CLI binary unchanged. The full CLI binary prints the combined version report (dispatcher version, binary version, local/global, local path when applicable).

### Session 2026-06-10 (Agent MCP Config)

- Q: Should `init` and adding an agent to project config include steps to create or update project-local agent MCP configuration files? → A: Yes. Both `init` and `config add-agent` MUST run agent-extension setup steps that create or idempotently update each selected agent's project-local native MCP configuration file(s) to reference the project-local `.spec-n-roll/cli/bin/spec-n-roll-mcp` binary (stdio transport). Each bundled agent extension declares its MCP config target path(s) and merge rules in its manifest; the generator preserves unrelated MCP server entries and updates or inserts the Spec-N-Roll entry only. `update` MUST refresh the Spec-N-Roll MCP binary path in all configured agents' project-local MCP config files when the local binary location or platform wrapper (`.cmd`) changes.

### Session 2026-06-10 (Agent Identifier Alignment)

- Q: What is the canonical extension `id` for GitHub Copilot — `copilot` or `github-copilot`? → A: **`copilot`**. Bundled out-of-the-box agent extension identifiers are kebab-case product slugs without vendor prefixes: `cursor`, `claude-code`, `copilot`, `codex`. Each extension's manifest `id`, install directory (`.spec-n-roll/bundled-extensions/{id}/`), `workflow.config.json` agent references, and generator module names MUST use the same `id` value. `github-copilot` is not a valid extension id — it may appear only as a human-readable `displayName` (e.g., "GitHub Copilot") or in native target paths (e.g., `.github/copilot-instructions.md`), not as the extension `id`.

### Session 2026-06-10 (Documentation Layout)

- Q: Where does toolkit-authored documentation live in the repository, and is it installed into user projects? → A: Toolkit-authored documentation lives in the repository root `docs/` directory only. It is for the Spec-N-Roll toolkit repository — `init`, `update`, and other project operations MUST NOT install or copy documentation into user projects. Documentation is excluded from the toolkit-owned vs. user-owned file classification and does not appear in the installed project layout.

### Session 2026-06-10 (Platform Scripts)

- Q: Is there a `scriptVariants` config field, `config script-variants` command, or `--script-variants` init flag? → A: No. Platform scripts are not configurable. `init` always installs paired `.sh` and `.ps1` automation scripts to `.spec-n-roll/scripts/`. At runtime the toolkit detects the current platform and executes the matching script form. There is no `scriptVariants` field in `workflow.config.json`, no `spec-n-roll config script-variants` subcommand, and no `--script-variants` flag.

---

## User Scenarios & Testing *(mandatory)*

### User Story 1 — Multi-Agent Project Initialization (Priority: P1)

A developer initializes Spec-N-Roll on a new or existing project and enables one or more agent extensions (cursor, claude-code, copilot, and codex ship out-of-the-box as extensions). The toolkit generates agent skills in `.agents/`, canonical rules in `.spec-n-roll/AGENTS.md`, thin per-agent pointer files in native locations, and project-local agent MCP configuration entries pointing at `.spec-n-roll/cli/bin/spec-n-roll-mcp` so any team member can immediately start a spec-driven workflow with their preferred agent — including MCP tools for deterministic mutations — with no manual cross-agent setup.

**Why this priority**: The ability to configure and use multiple agents is the primary differentiator of Spec-N-Roll from existing spec-driven toolkits. Without this, every other workflow step is blocked or degraded.

**Independent Test**: A developer can run `init`, select two agents, then open the project in both agent environments and verify that the full slash-command workflow (`/spec-n-specify`, `/spec-n-plan`, `/spec-n-tasks`, `/spec-n-implement`, `/spec-n-roll`) is available in each and that each agent's project-local MCP configuration references `.spec-n-roll/cli/bin/spec-n-roll-mcp` — without additional manual configuration.

**Acceptance Scenarios**:

1. **Given** a project with no agent configuration, **When** the developer runs initialization and selects multiple agents, **Then** agent-specific rule files, skill files, and project-local MCP configuration entries (pointing at `.spec-n-roll/cli/bin/spec-n-roll-mcp`) are created or updated for each selected agent and the core workflow commands are available in all of them.
2. **Given** a multi-agent project already initialized, **When** a new agent is added via a configuration command, **Then** the toolkit generates rules, skills, and project-local MCP configuration for the new agent without breaking the configuration of existing agents.
3. **Given** two developers using different agents on the same project, **When** each runs a workflow command, **Then** both receive consistent behavior, artifact naming, and file structure outcomes.
4. **Given** no supported AI agent is installed on the developer's machine, **When** the developer runs initialization and selects agents to configure, **Then** initialization completes successfully and agent-specific files are generated for the selected agents without any installation verification.

---

### User Story 2 — Platform-Appropriate Script Execution (Priority: P1)

A developer on any platform (Windows, macOS, Linux) can run Spec-N-Roll automation scripts without manually selecting the right script type. Initialization always installs paired `.sh` and `.ps1` scripts. At runtime the toolkit detects the current platform and runs the matching script form automatically.

**Why this priority**: Cross-platform reliability is a foundational requirement. Broken scripts on any platform destroy trust in the toolkit and block the entire workflow.

**Independent Test**: A developer on Windows can run a workflow that triggers automation scripts and verify that the `.ps1` variant executes; the same project on macOS runs the `.sh` variant without any configuration change.

**Acceptance Scenarios**:

1. **Given** a project initialized with spec-n-roll, **When** the toolkit runs on Windows, **Then** it automatically selects and executes the `.ps1` script for each automation operation.
2. **Given** a project initialized with spec-n-roll, **When** the toolkit runs on macOS or Linux, **Then** it automatically selects and executes the `.sh` script for each automation operation.
3. **Given** the required shell runtime is not available on the current platform, **When** a workflow step triggers an automation script, **Then** the toolkit fails with a clear error identifying the missing runtime and remediation steps.

---

### User Story 3 — Interactive Specification via Iterative Questioning (Priority: P1)

A developer describes a feature in plain language to start a `/spec-n-specify` session, which creates a new task spec directory. Rather than producing a complete spec from the first prompt alone, the agent interviews the developer with targeted, one-at-a-time questions — drilling into ambiguous areas, resolving branches of the decision tree, and suggesting answers — until the specification is complete and the developer is satisfied. The resulting spec is recorded as a formal artifact. `/spec-n-clarify` is a separate follow-up step for additional clarifications on an existing spec, using its own iterative interview process.

**Why this priority**: High-quality specifications are the foundation of all downstream work. An iterative, interactive session produces significantly higher quality specs than a single-prompt approach, reducing rework in planning and implementation.

**Independent Test**: A developer provides a vague one-sentence feature description. After a structured interview, a complete, unambiguous specification artifact is produced that passes all quality checklist items without manual editing.

**Acceptance Scenarios**:

1. **Given** a minimal feature description, **When** the specification session starts, **Then** the agent identifies the most ambiguous aspect and asks exactly one targeted question with suggested answers before proceeding.
2. **Given** the developer answers a question, **When** the answer is recorded, **Then** the agent moves to the next unresolved aspect of the spec — not re-asking resolved questions.
3. **Given** all critical questions are answered, **When** the session concludes, **Then** a complete specification artifact is written to the feature directory with no unresolved placeholders.
4. **Given** a sufficiently detailed initial description, **When** the session starts, **Then** the agent may skip questions it can answer confidently using reasonable defaults, noting those assumptions.

---

### User Story 4 — Complexity-Based Workflow Triage and Zero-Knowledge Continuation (Priority: P2)

When a developer starts a new workflow run via `/spec-n-specify <description>`, the toolkit captures the feature description and begins the `specify` step — the first step of every workflow tier. Triage is embedded at the start of `specify` (before the interview): it selects one of three default workflow tier variants — **papercut** (`specify → implement`), **quick** (`specify → tasks → implement`), or **full** (`specify → plan → tasks → implement`) — using a heuristic checklist (single-file/fix/copy → papercut; new behavior without architecture change → quick; cross-cutting/new subsystem/multi-actor → full), presents the matched tier with rationale, and requires developer confirmation or override. After triage confirms the tier, the specify interview proceeds and `workflowVariantId` is persisted. `implement` is always the tier exit.

`/spec-n-clarify` and `/spec-n-analyze` are on-demand steps invokable between any two tier steps; they are not part of default tiers and `/spec-n-roll` skips them unless explicitly run.

The developer can use `/spec-n-roll` as a zero-knowledge entry point: it detects new-spec vs continue intent (description argument → new spec; no description + Active specs → task-selection prompt; ambiguous state → "new or continue?" prompt), determines workflow state, and advances to the next tier step automatically.

**Why this priority**: Running a full spec-driven workflow for a typo fix creates friction and erodes developer trust. Scaling the workflow to task complexity ensures the toolkit feels natural for all task sizes. `/spec-n-roll` removes the cognitive overhead of navigating multi-step workflows.

**Independent Test**: Three feature descriptions of clearly different complexity (one-line bug fix, medium feature addition, major cross-cutting feature) each route to papercut, quick, and full respectively after triage confirmation. Running `/spec-n-roll` repeatedly on a mid-workflow project advances through each remaining tier step in order.

**Acceptance Scenarios**:

1. **Given** a feature description that matches papercut heuristics (e.g., single-file bug, UI copy change), **When** the developer runs `/spec-n-specify`, **Then** triage proposes the papercut tier and proceeds after confirmation.
2. **Given** a feature description indicating high complexity (cross-cutting concerns, new subsystems, multi-actor interactions), **When** the workflow is initiated, **Then** triage proposes the full tier and the developer is informed of all phases.
3. **Given** the toolkit selects a workflow tier, **When** the developer disagrees with the selection, **Then** the developer can explicitly override the triage decision and choose any available tier.
4. **Given** an empty or too-ambiguous feature description, **When** the toolkit cannot confidently triage, **Then** it presents all available workflow tiers and requires the developer to pick one manually, skipping automatic triage.
5. **Given** a workflow in progress with some steps completed, **When** the developer runs `/spec-n-roll`, **Then** the toolkit identifies the next incomplete tier step and executes it.
6. **Given** a completed workflow, **When** the developer runs `/spec-n-roll`, **Then** the toolkit informs the developer that the workflow is complete and no further steps remain.
7. **Given** multiple Active task specs exist, **When** the developer runs a pre-implement workflow command or `/spec-n-roll` without identifying a task, **Then** the agent presents a numbered list for the developer to choose (no silent default).
8. **Given** a developer invokes `/spec-n-roll` with a feature description argument, **When** the command runs, **Then** it routes to `/spec-n-specify` to begin a new spec (specify with embedded triage, then interview).

---

### User Story 5 — Living Specification Maintenance (Priority: P2)

The project maintains a set of living specification files written in Cucumber Gherkin syntax in a dedicated user-owned directory (`living-specs/`), named `living-specs/{kebab-case-domain}.feature` (e.g., `living-specs/user-authentication.feature`) — one file per application domain or capability area. The agent infers domain semantically from the feature description and automatically selects or creates the target file; planned living-spec creates and updates are documented in `plan.md` and `tasks.md` for developer review before implementation applies file changes. When implementation begins, living specs are updated to incorporate the new feature's scenarios before any test or production code is written. The living specs serve as the source of truth for what the application is and does.

**Why this priority**: Living specs provide persistent, human-readable documentation of application behavior that survives agent sessions, team changes, and codebase refactors. They are the bedrock of the TDD approach.

**Independent Test**: After three separate feature implementation cycles, a developer can read the living specs and understand the full current behavior of the application — with no stale scenarios — without inspecting the code. Behavioral contradictions are surfaced by Cucumber test runs, not a separate toolkit conflict-detection step.

**Acceptance Scenarios**:

1. **Given** no living specs exist, **When** the first feature implementation begins, **Then** the toolkit creates the initial Gherkin feature files representing the new feature's behavior.
2. **Given** living specs already exist, **When** a new feature implementation begins, **Then** the toolkit updates the existing Gherkin files to add new scenarios and amends any scenarios that the new feature changes, without removing unrelated scenarios.
3. **Given** a feature is removed or deprecated, **When** the corresponding living spec scenarios are updated, **Then** the removed scenarios are deleted from living spec files and the record is preserved in version control history only.
4. **Given** a living spec file, **When** a developer reads it without any other context, **Then** the scenarios are written in plain language sufficient for a non-technical stakeholder to understand what the system does.
5. **Given** a task spec with ID `001`, **When** the toolkit creates or updates living spec Gherkin scenarios for that task, **Then** each new or modified scenario is tagged with `@spec-n-roll-001` so that the test runner can filter and execute only the tests relevant to that task.
6. **Given** living spec scenarios tagged with multiple task IDs, **When** a developer instructs the test runner to execute tests for task `002`, **Then** only scenarios tagged `@spec-n-roll-002` are included in that run.
7. **Given** a scenario already tagged `@spec-n-roll-001`, **When** task `002` modifies that scenario, **Then** the scenario retains `@spec-n-roll-001` and gains `@spec-n-roll-002` — the prior tag is never removed.

---

### User Story 6 — Three-State Task Spec Lifecycle (Priority: P2)

Each workflow run creates a task-focused specification directory under `specs/{numeric-id}-{slug}/` scoped to that specific implementation. The numeric ID is unique, sequential, and auto-assigned from `project-metadata.json` → `nextTaskSpecId`; the slug is required (developer-provided or derived kebab-case from the feature description) and need not be unique. Both `taskSpecId` and `slug` are recorded in machine-readable JSON (`workflow-state.json`, `project-metadata.json`). Lifecycle status (**Active**, **Complete**, **Locked**) is persisted in `spec.md` YAML frontmatter. Tier-skipped artifacts (`plan.md`, `tasks.md`) are omitted — not created as stubs. Every task spec includes at minimum `spec.md` and `workflow-state.json`.

Multiple task specs may coexist in Active or Complete state simultaneously — `/spec-n-specify` does not lock prior specs. Only one Active task spec may be in the implement phase at a time. When `/spec-n-clarify` adds new un-implemented requirements to a Complete spec, the spec reverts to Active. Other Complete specs remain unlocked until the reverted spec begins a non-specify step or a different task spec begins a non-specify step. Locked task specs serve as a permanent, tamper-proof record of what was planned and built.

**Why this priority**: A three-state lifecycle balances audit trail integrity with post-completion flexibility. Locking at the start of the next implementation run (rather than at completion) gives developers a window to take additional actions on a finished spec before it is permanently sealed.

**Independent Test**: A developer completes a workflow run. They re-run test analysis (post-completion action) — the spec is not yet locked. They then start a new implementation-phase step — the prior spec locks. Any subsequent write attempt to the locked spec directory is rejected with a clear error.

**Acceptance Scenarios**:

1. **Given** a task spec in Active state, **When** the toolkit executes the final workflow step successfully, **Then** the task spec transitions to Complete state and the developer is informed they may still perform additional actions.
2. **Given** a task spec in Complete state, **When** the developer runs any workflow command that does not add new un-implemented requirements (e.g., re-run tests, `/spec-n-analyze`), **Then** the action executes and the spec remains in Complete state — it is not locked.
3. **Given** a task spec in Complete state, **When** the developer runs `/spec-n-clarify` and adds new un-implemented requirements, **Then** the requirements are appended and the task spec reverts to Active state; other Complete specs remain Complete.
4. **Given** one or more task specs in Complete state, **When** a different task spec begins any step beyond `/spec-n-specify`, **Then** all Complete task specs transition to Locked state before the new step proceeds.
5. **Given** a Complete spec that reverted to Active via clarify, **When** that spec begins a non-specify step, **Then** all other Complete task specs transition to Locked state before the step proceeds.
6. **Given** a task spec in Locked state, **When** any workflow command attempts to write to that spec directory, **Then** the toolkit raises a clear error and declines to modify the locked spec.
7. **Given** a Locked task spec, **When** a developer reads it, **Then** they can see the original requirements, plan, and task list exactly as they were when the spec was locked.
8. **Given** multiple `/spec-n-specify` invocations before any implementation step, **When** a developer inspects the project, **Then** multiple Active task spec directories coexist and none of the previously created specs have been locked.
9. **Given** one task spec is already in the implement phase, **When** a developer attempts to begin implementation on a different Active task spec, **Then** the toolkit declines with a clear error indicating only one implementation may be active at a time.

---

### User Story 7 — TDD Cucumber Test Suite Workflow (Priority: P2)

When implementation begins for a feature, the toolkit uses the living specification Gherkin `.feature` files in `living-specs/` as the Cucumber feature source and generates or updates step definitions and support code in the project's standard test location. Tests are written first (failing), then code is written to make them pass, following the red-green-refactor cycle. Unmapped Gherkin steps receive clearly marked stub step definitions. The toolkit guides the developer through each phase and reports test results at each cycle.

**Why this priority**: TDD with living-spec-driven Cucumber tests ensures that the application always behaves as specified, that regressions are caught immediately, and that the test suite documents real behavior — not implementation details.

**Independent Test**: A developer runs the implementation workflow for a feature with living spec Gherkin scenarios. Before writing any code, a failing test suite exists. After implementation, all tests pass. The tests are written against the public interface of the application, not internal implementation.

**Acceptance Scenarios**:

1. **Given** living spec Gherkin scenarios for a feature, **When** implementation begins, **Then** a failing test suite (red) exists — Cucumber runs against the living spec `.feature` files with step definitions (including stubs for unmapped steps) in the standard test location — before any production code is written.
2. **Given** a failing test suite, **When** the developer implements the feature, **Then** the toolkit tracks which tests are now passing and presents progress until all tests go green.
3. **Given** all tests passing, **When** the developer refactors, **Then** the toolkit validates that tests remain green after each refactor step.
4. **Given** a test generated from a Gherkin scenario, **When** the implementation changes internally without changing behavior, **Then** the test continues to pass without modification (tests are behavior-focused, not implementation-focused).

---

### User Story 8 — Extensible and Configurable Workflow Definitions (Priority: P3)

A developer or tooling author can replace or extend any individual step in the Spec-N-Roll workflow with a custom implementation, without forking or modifying the toolkit core. Workflow variants — named, ordered compositions of steps — are defined in project configuration files; each tier variant lists a shared `specify` step (with embedded triage) as step 1 plus its tier-specific tail. Multiple workflow definitions can coexist and share step references. Extension `entrypoint` values are in-process Node modules (`import()` + exported handler). Extensions are a first-class feature documented as thoroughly as the built-in workflow.

**Why this priority**: Extensibility is what transforms Spec-N-Roll from a fixed workflow tool into a platform. Teams with unique processes (JIRA-triggered specs, compliance gates, custom triage logic) can integrate without maintaining forks. Configurable workflow files make the system inspectable and adjustable without code changes.

**Independent Test**: A developer inspects the default `workflow.config.json` and finds papercut, quick, and full tier variants — each listing a shared `specify` step as step 1. They register a custom extension that replaces the built-in triage logic within `specify`. Running `/spec-n-specify` on a simple task selects papercut after confirmation; on a complex task it selects full. Both quick and full variants share the same built-in `tasks` and `implement` step references.

**Acceptance Scenarios**:

1. **Given** a custom workflow step registered as an extension, **When** the toolkit processes that step ID, **Then** the custom step is invoked instead of the built-in step.
2. **Given** multiple extensions contributing to the same step ID, **When** the workflow runs, **Then** priority ordering determines which extension is active and the developer is informed of the active handler for each step.
3. **Given** an extension is disabled, **When** the workflow runs, **Then** the built-in handler for that step ID executes without any intervention from the disabled extension.
4. **Given** a workflow variant definition that combines a custom specification step with the built-in plan and tasks steps, **When** the developer runs that variant, **Then** the correct mix of custom and built-in steps executes in the defined order.
5. **Given** multiple named workflow variants defined in the project configuration, **When** triage within `specify` evaluates the current task, **Then** it selects one workflow variant and the remaining steps of that variant are executed in order after `specify` completes.
6. **Given** a freshly initialized project, **When** the developer inspects the workflow configuration, **Then** default papercut, quick, and full tier variants are present — each with a shared `specify` step reference as step 1 and `implement` as exit — and `defaultWorkflowId` pre-selects a tier in the manual picker only.
7. **Given** two workflow variants that both include the `plan` and `tasks` steps, **When** both are defined in the configuration, **Then** they reference the shared step definition rather than duplicating it.

---

### User Story 9 — CLI-Based Toolkit Setup, Update, and Configuration (Priority: P2)

A developer uses a TypeScript CLI application as the primary non-agent interface for managing their Spec-N-Roll installation. The CLI handles the initial setup of the toolkit in a project, upgrades to newer toolkit versions, and modification of the toolkit configuration (e.g., adding agents). The CLI is the authoritative tool for any operation that modifies toolkit-owned files.

**Why this priority**: A dedicated CLI separates toolkit management concerns from workflow execution, provides a reliable upgrade path, and ensures that configuration changes are performed through a validated interface rather than by hand-editing files.

**Independent Test**: A developer uses the CLI to initialize a project, later runs an update command to apply a new toolkit version, and confirms that their user-owned config files are preserved while toolkit-owned files are updated to the new version.

**Acceptance Scenarios**:

1. **Given** an empty project directory, **When** the developer runs the CLI setup command, **Then** all toolkit-owned files are written to the project, a default workflow config is created, paired platform scripts are installed, and the developer is prompted to select agents.
2. **Given** an initialized project on an older toolkit version, **When** the developer runs the CLI update command, **Then** toolkit-owned files are updated to the new version and all user-owned files (config, extensions, custom steps) are preserved without modification.
3. **Given** a developer who wants to add a new agent to an existing project, **When** they run the CLI modify command, **Then** the toolkit generates the appropriate rules, skill files, and project-local MCP configuration entry for the new agent without altering any other existing configuration.
4. **Given** a toolkit update that includes a config schema change, **When** the developer runs the update command, **Then** the CLI reads all user-owned config files using the tolerant reader, migrates them to the current schema, writes them back to disk, and reports which files were updated.
5. **Given** a project with a locally installed full CLI binary, **When** the developer invokes the globally installed dispatcher from within that project directory, **Then** the dispatcher exec's the local binary without loading global full CLI code.
6. **Given** a developer who needs to run the global full CLI regardless of local installation, **When** they invoke the dispatcher with `--global`, **Then** the global full binary executes and the local binary is not consulted.
7. **Given** any dispatcher invocation, **When** the developer passes `-v` or `--version`, **Then** the dispatcher forwards the flag to the resolved full CLI binary, which prints dispatcher version, executed binary version, `local` or `global` target, and the absolute local binary path when local.
8. **Given** agent MCP configuration in an initialized project, **When** MCP tools run, **Then** they invoke `.spec-n-roll/cli/bin/spec-n-roll-mcp` only — not the global dispatcher or the full CLI binary.

---

### User Story 10 — Versioned Toolkit with Safe Update Model (Priority: P2)

Every release of the Spec-N-Roll toolkit carries a version identifier. Files in the project are classified as either toolkit-owned (updated when the toolkit version changes) or user-owned (always preserved on update). The classification is unambiguous — a file cannot be in both categories. Config files use a versioned schema that guarantees backward compatibility: projects on older config schemas continue to work after updating, and the CLI provides migration assistance when breaking schema changes are unavoidable.

**Why this priority**: Without a clear ownership boundary and versioning model, toolkit updates are dangerous — they risk silently overwriting user customizations. A safe update model is prerequisite to teams trusting the toolkit enough to keep it current.

**Independent Test**: A developer customizes their workflow config file and registers a custom extension. After a toolkit update that includes both a core skill file change and a config schema version bump, their custom config and extension are untouched and the project still works. The updated skill file reflects the new version.

**Acceptance Scenarios**:

1. **Given** the toolkit is at a known version, **When** the developer queries the CLI, **Then** the current installed version is reported and the latest available version is shown for comparison.
2. **Given** toolkit-owned files and user-owned files coexist in the project, **When** a toolkit update is applied, **Then** only toolkit-owned files are modified and user-owned files remain byte-for-byte identical to their pre-update state.
3. **Given** a config file written for an older schema version, **When** the developer runs a CLI update, **Then** the CLI reads the old config using the tolerant reader, migrates it to the current schema, and writes the updated config back to disk — all on-disk configs are at the current schema version when the update completes.
4. **Given** a toolkit update that introduces a breaking config schema change, **When** the developer applies the update, **Then** the CLI presents a migration plan and requires explicit confirmation before modifying any user-owned config files.
5. **Given** an installed extension whose manifest targets an older toolkit version, **When** the developer runs a CLI update to a newer toolkit version, **Then** the CLI evaluates the extension against the toolkit's compatibility declarations, and if a mismatch is found, reports it as a warning in the update summary — the update completes and the extension continues to execute without being blocked.
6. **Given** a developer has locally modified a toolkit-owned file, **When** a toolkit update would overwrite that file, **Then** the CLI saves a `.bak` copy of the modified file alongside it, overwrites the file with the new toolkit version, and reports the conflict in the update summary so the developer can reconcile their changes.

---

### Edge Cases

- **No agents installed at init**: Initialization completes based on developer-selected agents; no installation verification is performed.
- **No script runtime on platform**: The toolkit fails with a clear error naming the missing shell runtime and remediation steps.
- **Interrupted workflow**: Partial artifacts are detected via a built-in step output manifest (expected files per step ID vs. `workflow-state.json` completion). `/spec-n-roll` re-presents the three-choice prompt (restart / cancel / force-clean). Cancelling leaves artifacts in place for inspection.
- **Tier-skipped artifacts**: `plan.md` and `tasks.md` are absent on tiers that skip those steps — not an error; detection respects the selected tier's step list.
- **Living spec scenario conflicts**: No toolkit conflict-detection step. Contradictions are surfaced by automated Cucumber test runs.
- **Empty or ambiguous feature description**: All workflow tiers are presented; the developer must pick one manually, skipping triage.
- **Unmapped Gherkin step definitions**: Stub step definitions are generated in the failing test suite, clearly marked as stubs.
- **State file vs. artifact mismatch**: The state file wins when present and parseable; the developer is warned and must confirm once before proceeding.
- **Multiple Active task specs**: Pre-implement commands and `/spec-n-roll` present a numbered-list interactive prompt when no task is identified — no silent default.
- **New vs continue on `/spec-n-roll`**: Description argument starts a new spec; ambiguous state prompts "new or continue?".
- **Deleted `.bak` conflict file**: No other toolkit record exists; the developer must rely on version control history if the backup was deleted.
- **Extension runtime failure after upgrade warning**: The current workflow step fails with a clear error citing the extension and prior warning. Warnings are not retroactively upgraded to block startup.
- **Custom extension step IDs**: Workflows may reference step IDs introduced only by extensions; tier definitions and extension manifests together form the authoritative step registry.
- **Extension upgrade integration**: No `before_update`/`after_update` hook events; extensions that need upgrade-time behavior document CLI-based integration instead.
- **Hook for unknown step ID**: At manifest load/registration, the toolkit warns when a hook's `{stepId}` is not in the merged step registry and skips dispatch for that hook; this is non-blocking and does not fail workflow execution.
- **MCP/CLI version skew**: The project-local MCP server and full CLI binary MUST share the same toolkit version; `init` registers the bundled pair together. Mismatches surface as errors with remediation to re-run update or init.
- **Dispatcher exec failure**: If the dispatcher resolves a local binary but exec fails (missing, corrupt, permission denied), the dispatcher MUST fail with a clear error — it MUST NOT silently fall back to the global full binary unless `--global` was passed.
- **MCP via global dispatcher**: MCP agent configuration MUST point at `.spec-n-roll/cli/bin/spec-n-roll-mcp`, not the globally installed dispatcher or full CLI binary.
- **Pre-existing agent MCP config**: When an agent's project-local MCP config file already exists, `init` and `config add-agent` MUST merge idempotently — insert or update only the Spec-N-Roll MCP server entry; preserve all other MCP server entries and unrelated configuration.
- **Stale MCP path after update**: When `update` changes the local MCP binary path or wrapper, all configured agents' project-local MCP config files MUST be refreshed; mismatches surface as errors with remediation to re-run `update`.
- **No local binary in tree**: When walk-up finds no `.spec-n-roll/cli/bin/spec-n-roll` ancestor (and `--global` not passed), the dispatcher exec's the co-bundled global full CLI binary resolved relative to the dispatcher's install path.
- **Direct full CLI invocation**: When the full CLI binary is invoked directly (not via dispatcher), `-v`/`--version` reports binary version and indicates direct invocation (no dispatcher context).
- **Agent direct-edit of machine-readable state**: Attempts to modify `workflow-state.json`, frontmatter, `project-metadata.json`, or task checkboxes outside MCP/CLI are out of contract — skills MUST route through MCP tools; the core library is the sole writer for these fields.
- **Step output before instantiation**: Agents MUST instantiate step output templates via MCP/CLI before editing prose; editing a non-existent step output file is out of contract.
- **Frontmatter edit via prose**: Agents MUST NOT change YAML frontmatter by editing `spec.md` directly — frontmatter updates use MCP/CLI only.
- **Living specs outside MCP/CLI scope**: Living spec file creates and Gherkin edits are agent-direct; deterministic MCP/CLI commands do not apply to `living-specs/`.

---

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: The toolkit MUST support configuration of one or more AI coding agents during initialization via extensions only — no hard-coded agent registry. Cursor, claude-code, copilot, and codex MUST ship as out-of-the-box extensions at `.spec-n-roll/bundled-extensions/{id}/`, referenced by default in `workflow.config.json`. Each agent extension manifest MUST declare project-local MCP configuration target path(s) and merge rules. On `init` and `config add-agent`, the toolkit MUST generate skills in `.agents/`, canonical rules in `.spec-n-roll/AGENTS.md`, thin per-agent pointer files in native locations, and create or idempotently update each selected agent's project-local native MCP configuration file(s) to reference `.spec-n-roll/cli/bin/spec-n-roll-mcp` (stdio transport) — preserving unrelated MCP server entries. The toolkit MUST NOT verify that selected agents are installed.
- **FR-002**: The toolkit MUST allow seamless use of any configured agent on a project — switching between agents MUST NOT require manual reconfiguration of workflow commands.
- **FR-003**: Initialization MUST install paired automation scripts in both shell (`.sh`) and PowerShell (`.ps1`) forms to `.spec-n-roll/scripts/`. There is no per-project configuration to enable or disable script forms.
- **FR-004**: The toolkit MUST automatically detect the current platform and execute the matching script form (`.ps1` on Windows, `.sh` on Unix-like systems) without developer intervention. When the required shell runtime is not available on the current platform, the toolkit MUST fail with a clear error identifying the missing runtime and remediation steps.
- **FR-005**: The toolkit MUST support interactive, iterative specification sessions where the agent asks one targeted question at a time, provides suggested answers, and waits for developer input before proceeding. `/spec-n-specify` MUST include this interview when creating a new task spec. `/spec-n-clarify` MUST run a separate interview for follow-up clarifications on an existing spec.
- **FR-006**: `specify` MUST be the first step of every workflow tier. Triage MUST be embedded at the start of the `specify` step (before the interview) — not a separate workflow step. Triage MUST use a built-in heuristic checklist (single-file/fix/copy → papercut; new behavior without architecture change → quick; cross-cutting/new subsystem/multi-actor → full), present the matched tier with rationale, require developer confirmation or override, and persist `workflowVariantId` before the interview proceeds. Default tiers: **papercut** (`specify → implement`), **quick** (`specify → tasks → implement`), **full** (`specify → plan → tasks → implement`). `implement` MUST be every tier's exit. When the feature description is empty or too ambiguous to triage, the toolkit MUST present all tiers and require manual selection (pre-selecting `defaultWorkflowId`), skipping automatic triage.
- **FR-007**: The developer MUST be able to override the triage-selected workflow tier.
- **FR-008**: Living specifications MUST be authored and maintained as Cucumber Gherkin `.feature` files in user-owned `living-specs/`, named `living-specs/{kebab-case-domain}.feature`, with one file per application domain or capability area. The agent MUST infer domain semantically and automatically select or create the target file. Living spec files are fully agent-managed — creates and updates do NOT go through MCP/CLI (unlike machine-readable task spec state and template-instantiated step outputs). Planned living-spec creates and updates MUST be documented in `plan.md` and `tasks.md` for developer review before implementation applies file changes. They MUST represent the complete, current accepted behavior of the application. Deprecated scenarios MUST be removed from living spec files; version control history is the archival record.
- **FR-009**: Living specification files MUST be updated at implementation entry before any test or production code is written. The tasks step MUST list updating living specs as its first task(s). The toolkit MUST attempt to update all relevant living specs as new functionality is defined. Behavioral contradictions MUST be surfaced by automated Cucumber test runs — not a separate toolkit conflict-detection step.
- **FR-010**: Task-focused specification artifacts MUST be created as a new directory under `specs/{numeric-id}-{slug}/` for each workflow run. The toolkit MUST auto-assign a unique, sequential, zero-padded numeric `taskSpecId` from `project-metadata.json` → `nextTaskSpecId` (e.g., `001`). The developer MAY provide a slug; if omitted, the toolkit MUST derive a kebab-case slug from the feature description. The slug is **required** and recorded alongside `taskSpecId` in machine-readable JSON (`workflow-state.json`, `project-metadata.json`). Slugs are not required to be unique. Tier-skipped artifacts (`plan.md`, `tasks.md`) MUST be omitted — not created as stubs. Every task spec MUST include at minimum `spec.md` (with YAML frontmatter including `status`) and `workflow-state.json`.
- **FR-011**: Task-focused specification directories follow a three-state lifecycle persisted in `spec.md` YAML frontmatter: Active → Complete → Locked. The toolkit MUST transition `status` to Complete automatically when the final workflow step executes. Any workflow command MAY run against a Complete spec until locking. When `/spec-n-clarify` adds new un-implemented requirements, `status` MUST revert from Complete to Active. The toolkit MUST transition all Complete task specs to Locked when a different task spec begins any step beyond `/spec-n-specify`, or when a reverted spec begins a non-specify step. `/spec-n-specify` MUST NOT trigger locking of prior task specs. Only one Active task spec MAY be in the implement phase at a time. Locked task specs MUST be immutable — the toolkit MUST refuse any write attempts to a Locked task spec directory.
- **FR-012**: When implementation begins, Cucumber MUST run against the living spec Gherkin `.feature` files directly. Step definitions and executable test code MUST reside in the project's standard test location. The toolkit MUST generate stub step definitions for unmapped Gherkin steps, clearly marked as stubs. Tests MUST be failing (red) before any production code is written.
- **FR-013**: Generated tests MUST target the public behavior interface of the application, not internal implementation details, so they survive internal refactors without modification.
- **FR-014**: Every workflow step MUST be independently replaceable by an extension, without modifying the toolkit core. Step identifiers MUST NOT be closed by the manifest schema — workflows define which step IDs exist, including custom steps introduced by extensions.
- **FR-015**: The toolkit MUST provide a documented extension mechanism that supports registering custom workflow steps (by open `stepId`), hooks, triage logic (within `specify` or as replaceable built-in behavior), and workflow variant definitions in project configuration files. Extension manifest `steps` entries MUST use a `stepId` field (kebab-case string, not a fixed enum) identifying the workflow step slot this extension replaces or augments. Extension manifest `hooks` MUST use only `before_{stepId}` and `after_{stepId}` event names for workflow step IDs; the schema MUST NOT enumerate hook events and MUST NOT support CLI lifecycle hooks (e.g., no `before_update`/`after_update`). At manifest load/registration, hooks whose `{stepId}` is not in the merged step registry MUST produce a warning and MUST be skipped at dispatch — non-blocking. `/spec-n-roll` MUST fire hooks for the underlying step only — no meta-step hooks. Toolkit upgrade integration for extensions uses CLI commands and documented extension mechanisms, not hook events. Extension `entrypoint` values MUST be project-relative paths to JS/TS modules exporting a standard handler, invoked in-process via `import()`.
- **FR-016**: Documentation MUST be treated as a first-class deliverable of the toolkit. It MUST comprehensively cover: the overall workflow (all steps and their interactions), the extension/plugin mechanism (step replacement, hooks, workflow variant definition, triage steps), multi-agent setup and switching, platform script execution (`.sh`/`.ps1` auto-selection), CLI usage (setup, update, modify), and the versioning and migration model. Documentation MUST be sufficient for a developer to adopt and extend the toolkit without consulting source files. Documentation MUST be authored as Markdown files in the toolkit repository root `docs/` directory only; `init`, `update`, and other project operations MUST NOT install or copy documentation into user projects. Documentation is NOT subject to the file ownership classification. Extension/plugin documentation MUST include, at minimum: a quick-start guide, a reference section covering the manifest schema, hook events, and step interfaces, and at least one fully worked extension example.
- **FR-017**: All agent workflow commands MUST use the `spec-n-` prefix (e.g., `/spec-n-specify`, `/spec-n-plan`, `/spec-n-tasks`, `/spec-n-implement`, `/spec-n-clarify`, `/spec-n-analyze`, `/spec-n-roll`).
- **FR-018**: A `/spec-n-roll` meta-command MUST exist that determines the current workflow and its progress, then executes the next appropriate tier step — skipping on-demand `clarify` and `analyze` unless explicitly run. It MUST detect new-spec vs continue intent: a description argument routes to `/spec-n-specify`; no description with zero Active specs prompts for a description; no description with Active specs presents a numbered task-selection prompt; ambiguous state prompts "new or continue?". At implement time, `/spec-n-roll` MUST use `.spec-n-roll/config/project-metadata.json` to identify the current task spec. When multiple Active task specs exist and no task is identified, all pre-implement commands and `/spec-n-roll` MUST present the same numbered-list interactive prompt (no silent default).
- **FR-019**: When a task spec creates or updates living specification Gherkin scenarios, the tag `@spec-n-roll-{numeric-id}` (e.g., `@spec-n-roll-001`) MUST be added to every new or modified scenario. Tags MUST be additive — if a scenario is later modified by task `002`, `@spec-n-roll-001` MUST be preserved and `@spec-n-roll-002` MUST be added alongside it. Tags are never removed from a scenario. This enables test runners to filter by any task that created or modified a scenario.
- **FR-020**: Named workflow variants MUST be definable in `.spec-n-roll/config/workflow.config.json`. Initialization MUST write default papercut, quick, and full tier variants — each listing a shared `specify` step reference as step 1 plus its tier-specific tail. `defaultWorkflowId` MUST pre-select a tier in the manual/override picker only; it MUST NOT auto-run a workflow before `specify`. Steps MUST be reusable across variants without duplication. `/spec-n-clarify` and `/spec-n-analyze` MUST be available as on-demand steps between tier steps but MUST NOT appear in default tier definitions.
- **FR-021**: The toolkit MUST write `workflow-state.json` to the task spec directory after each step completes, recording `taskSpecId`, required `slug`, active workflow variant, operational `status` (`active`/`paused`/`complete`), and the last completed step. Workflow state operational `status` tracks execution progress (`paused` = interrupted mid-step); task spec lifecycle (`Active`/`Complete`/`Locked`) is persisted separately in `spec.md` YAML frontmatter; both align to `complete`/`Complete` when `implement` finishes. Artifact detection MUST respect the selected tier's step list (tier-skipped files are not expected). When the state file is present and parseable but conflicts with artifacts on disk, the state file MUST take precedence with a single confirmation prompt. If absent or unreadable, `/spec-n-roll` MUST fall back to artifact detection.
- **FR-022**: When `/spec-n-roll` detects partial artifacts for the current incomplete step — defined by a built-in step output manifest mapping step IDs to expected output files, where partial means any expected file exists but the step is not recorded complete in `workflow-state.json` — it MUST warn the developer and present a three-choice prompt: **restart** (overwrite all partial artifacts and restart the step from the beginning), **cancel** (leave all partial artifacts in place for manual inspection — the workflow remains in a paused state resumable via `/spec-n-roll`), or **force-clean** (delete all partial artifacts, then restart the step from the beginning). One prompt covers all partial files for that step — the toolkit MUST NOT prompt per-artifact.
- **FR-023**: The toolkit MUST provide a TypeScript CLI as the primary human interface for toolkit and workflow operations. The globally installed npm artifact MUST be a **lightweight dispatcher** containing only logic to resolve whether to exec the project-local full CLI binary or the global full CLI binary by walking up parent directories from `cwd` to find the nearest `.spec-n-roll/cli/bin/spec-n-roll`; it MUST NOT import or load the full CLI, core library, or MCP code when dispatching to local. The **full CLI binary** (argument parsing, subcommands, Ink UI, update, config) lives at `.spec-n-roll/cli/bin/spec-n-roll` (with `spec-n-roll.cmd` on Windows) when project-local, and as a co-bundled binary resolved relative to the dispatcher install path when no local binary exists (or when `--global` is passed). The full CLI MUST support two invocation modes: **interactive** — bare `spec-n-roll` spawns Ink; **non-interactive** — `spec-n-roll <subcommand> [args]` runs synchronously and exits. Every deterministic mutation exposed as an MCP tool MUST have a parallel CLI subcommand invoking the same core-library operation. The CLI MUST be the only supported mechanism for applying toolkit updates. `--global` MUST force the global full binary and skip local resolution. The dispatcher MUST forward `-v`/`--version` unchanged to the resolved full CLI binary. The full CLI binary MUST print a combined version report: dispatcher version, executed binary version, `local`/`global` target, and absolute local binary path when applicable (dispatcher passes invocation context when exec'ing the child).
- **FR-030**: The toolkit MUST ship a project-local MCP server as a separate binary at `.spec-n-roll/cli/bin/spec-n-roll-mcp` (with `spec-n-roll-mcp.cmd` on Windows). `init` and `config add-agent` MUST create or idempotently update each configured agent's project-local native MCP configuration file(s) to reference this binary (stdio transport) per the agent extension's declared targets and merge rules. `update` MUST refresh the Spec-N-Roll MCP entry in all configured agents' project-local MCP config files when the local binary path or platform wrapper changes. MCP MUST always use this local binary — never the global dispatcher or the full CLI binary directly. The MCP server and full CLI MUST be thin interfaces over the shared local core library — deterministic mutations MUST NOT be duplicated across interfaces. `init` MUST install both the full CLI and MCP binaries into `.spec-n-roll/cli/bin/`.
- **FR-031**: Agent skills MUST NOT instruct agents to directly modify machine-readable workflow state. Changes to `workflow-state.json`, `spec.md` YAML frontmatter (`status`), `project-metadata.json`, and task completion checkboxes in `tasks.md` MUST be performed only via MCP tools (agents) or parallel CLI subcommands (developers), invoking the shared core library. Each such mutation MUST have a dedicated MCP tool and matching CLI subcommand. Agents MAY directly edit prose bodies in `spec.md`, `plan.md`, and `tasks.md` after instantiation.
- **FR-032**: Step output files (`spec.md`, `plan.md`, `tasks.md`, etc.) MUST be created from toolkit-owned templates via MCP/CLI instantiate commands that copy template files into the task spec directory. Templates MUST contain inline instructions guiding how to fill each section. Frontmatter values required at creation MUST be passed as command arguments. After instantiation, agents edit prose content directly in the file; frontmatter updates MUST use MCP/CLI commands. Agent skills MUST document the instantiate command and content-editing workflow — not duplicate template structure or fill instructions already present in the template file.
- **FR-024**: Every toolkit release MUST carry a version identifier following semantic versioning (MAJOR.MINOR.PATCH). A breaking change to any extension interface — including step contracts, hook event shapes, or the manifest schema — MUST trigger a toolkit major version bump. Extension authors rely on the toolkit's semver guarantee; extension interfaces are not individually versioned. The CLI MUST be able to report the currently installed version and the latest available version, and MUST provide a command to apply updates. Each extension manifest MUST declare the specific toolkit version it was designed for. The toolkit MUST maintain a compatibility declaration listing toolkit version / extension version combinations it knows to be incompatible. During a CLI update, the CLI MUST evaluate all installed extensions against these compatibility declarations and report any mismatches as warnings. Version mismatch warnings MUST NOT block the update or block subsequent workflow execution.
- **FR-025**: Every file written to a project by the toolkit MUST be classified as either toolkit-owned or user-owned, with the two categories being mutually exclusive. Toolkit-authored documentation in the repository root `docs/` directory is excluded — it is not written to user projects by `init` or `update`. **Toolkit-owned**: `.spec-n-roll/` (CLI, scripts, `AGENTS.md`, `compatibility.json`, `bundled-extensions/`) and `.agents/` (generated skills). **User-owned**: `.spec-n-roll/config/` (including `workflow.config.json`, `project-metadata.json`, `extensions/`), `specs/`, and `living-specs/`. A file's location determines ownership — no per-file override. Toolkit-owned files are overwritten on update (with `.bak` backup if locally modified); user-owned files are preserved unless an explicit confirmed config migration applies.
- **FR-026**: All config files MUST carry a schema version identifier. The CLI MUST maintain an additive-only "read schema" (tolerant reader) capable of parsing config files written for any prior schema version. Schema migration MUST occur at CLI update time: the CLI reads all user-owned config files using the tolerant reader, migrates them to the current schema, and writes the updated configs back to disk before completing the update. Breaking schema changes between toolkit versions are permitted. At runtime (outside of CLI update), the toolkit MAY assume all configs conform to the current schema version.
- **FR-027**: The toolkit MUST maintain `.spec-n-roll/config/project-metadata.json` with `nextTaskSpecId` (sequential counter incremented on each new task spec), and fields identifying the current task spec for implementation (`taskSpecId` numeric + required `slug`). Implementation metadata MUST be validated and set when the implement step begins.
- **FR-028**: `/spec-n-analyze` MUST perform cross-artifact consistency and quality analysis across the current task spec's spec, plan, tasks, and optionally living specs, producing a non-destructive report of gaps, contradictions, and checklist failures.
- **FR-029**: When an extension fails at runtime after an upgrade-time compatibility warning, the toolkit MUST fail the current workflow step with a clear error citing the extension and prior warning, with remediation guidance. Upgrade warnings MUST NOT be retroactively upgraded to block startup.

### Key Entities

- **Agent Configuration**: Represents a configured AI coding agent delivered via an extension (bundled or user-installed). The extension `id` is the stable kebab-case key used in manifests, `workflow.config.json`, and `.spec-n-roll/bundled-extensions/{id}/` (v1 OOTB ids: `cursor`, `claude-code`, `copilot`, `codex`). Skills live in `.agents/`; canonical rules live in `.spec-n-roll/AGENTS.md` with thin per-agent pointer files in native locations. Each agent extension declares project-local MCP configuration target path(s) and merge rules; `init`, `config add-agent`, and `update` create or update those files to reference `.spec-n-roll/cli/bin/spec-n-roll-mcp`.
- **Living Specification**: A Cucumber Gherkin `.feature` file at `living-specs/{kebab-case-domain}.feature` representing current accepted behavior of one domain area. Fully agent-managed (no MCP/CLI template instantiation). Agent infers domain semantically and auto-selects or creates the target file; planned changes appear in `plan.md`/`tasks.md` before implementation. Tagged with `@spec-n-roll-{numeric-id}`.
- **Task Spec**: A directory at `specs/{numeric-id}-{slug}/` containing at minimum `spec.md` (YAML frontmatter with `status: Active|Complete|Locked`) and `workflow-state.json`, plus tier-applicable artifacts (`plan.md`, `tasks.md` only when the selected tier includes those steps). `taskSpecId` is numeric; `slug` is required in JSON and directory path.
- **Project Metadata**: Versioned file at `.spec-n-roll/config/project-metadata.json` with `nextTaskSpecId` counter and current implementation task (`taskSpecId` + `slug`). Set when implement begins.
- **Workflow Step**: A single executable unit in the spec-driven development process, identified by an open kebab-case `stepId` (e.g., `specify`, `clarify`, `plan`, `tasks`, `implement`, or custom extension-defined IDs). Steps are declared per workflow in `workflow.config.json` and extension manifests; the schema does not close the set of valid IDs. Independently replaceable via extensions.
- **Workflow Variant**: A named, ordered composition of workflow steps defined in a project configuration file, each tier variant listing a shared `specify` step (with embedded triage) as step 1 plus a tier-specific tail. Multiple variants share step references without duplication. Triage within `specify` selects which variant's tail executes after `specify` completes.
- **Workflow State**: Persisted in `specs/{id}-{slug}/workflow-state.json`. Tracks `taskSpecId`, required `slug`, operational `status` (`active`/`paused`/`complete`), workflow variant, and last completed step. Independent from task spec lifecycle (`spec.md` frontmatter) but aligned on completion. Falls back to tier-aware artifact detection when absent or unreadable.
- **Extension**: A registered, self-contained module that provides custom workflow step handlers (referenced by open `stepId`), dynamic `before_{stepId}`/`after_{stepId}` hooks, and optional workflow variants, invoked in-process via Node `import()` of a relative JS/TS module exporting a standard handler. Each extension manifest MUST declare the specific toolkit version it was designed for. This declaration is used at upgrade time to surface compatibility warnings — it does not block execution.
- **Triage (embedded in specify)**: Built-in logic at the start of the `specify` step that evaluates the feature description and selects a workflow tier variant (papercut/quick/full). Replaceable via extensions targeting the `specify` step ID. Not a separate workflow step in tier definitions.
- **Triage Assessment**: The output of triage within `specify` — the evaluation of a feature description that determines which workflow variant tail to activate (e.g., papercut / quick / full tier).
- **Platform Script**: A paired automation script installed at `.spec-n-roll/scripts/` in both `.sh` (Unix-like) and `.ps1` (Windows) forms for the same logical operation; runtime platform detection selects which form to execute.
- **CLI Dispatcher**: Globally installed lightweight binary containing only local-vs-global resolution logic. Walks up from `cwd` to find the nearest local full CLI binary; falls back to co-bundled global full CLI resolved relative to dispatcher install path. Exec's the full CLI binary as a child process with invocation context; does not load full CLI/core/MCP code in-process when dispatching local. Forwards `-v`/`--version` unchanged to the full CLI binary, which emits the combined version report.
- **CLI Tool** (full binary): Project-local (or global fallback) binary with argument parsing, subcommands, Ink UI, and update/config operations. Bare `spec-n-roll` spawns interactive Ink; `spec-n-roll <subcommand> [args]` runs non-interactively. Shares the core library with the MCP server. Installed at `.spec-n-roll/cli/bin/spec-n-roll`.
- **MCP Server**: Project-local stdio MCP server binary at `.spec-n-roll/cli/bin/spec-n-roll-mcp`. Referenced from each configured agent's project-local native MCP configuration file(s), created or updated by `init`, `config add-agent`, and refreshed by `update`. Separate from the full CLI binary; never uses the global dispatcher. Thin interface over the shared core library.
- **Core Library**: Shared local library implementing all deterministic mutations (workflow state transitions, task spec lifecycle status, project metadata updates, task checkbox toggles, template instantiation by file copy, frontmatter updates, etc.). Invoked by both the MCP server and CLI. Agent skills MUST direct agents to MCP tools for machine-readable state and template instantiation — not direct file edits to `workflow-state.json`, frontmatter, `project-metadata.json`, or `tasks.md` checkboxes.
- **Step Output Template**: Toolkit-owned template file for a workflow step's output artifact (e.g., `spec.md`, `plan.md`). Contains inline fill instructions for each section. Instantiated into a task spec directory via MCP/CLI copy command with optional frontmatter arguments. Prose content is agent-edited after instantiation; frontmatter is MCP/CLI-only.
- **Toolkit Version**: A release identifier following semantic versioning (MAJOR.MINOR.PATCH) attached to every toolkit distribution. A breaking change to any extension interface requires a major version bump. Recorded in the project after initialization and updated by the CLI on upgrade. Used to detect when project files are out of sync with the installed toolkit. Extension authors rely on the toolkit's semver guarantee to know which toolkit versions are safe to target without breaking changes.
- **File Ownership Class**: Toolkit-owned: `.spec-n-roll/` (except `config/`) and `.agents/`. User-owned: `.spec-n-roll/config/`, `specs/`, `living-specs/`. Toolkit-authored documentation in the repository root `docs/` is excluded and is not installed into user projects.
- **Config Schema Version**: A version identifier embedded in every config file recording which toolkit schema version it conforms to. During a CLI update, the CLI reads all configs using an additive-only tolerant reader, migrates them to the current schema, and writes the result back to disk. After an update completes, all on-disk configs are at the current schema version. Runtime toolkit code may assume configs are at the current version.

---

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: A developer can initialize Spec-N-Roll with multiple agents and run a full workflow command in each configured agent environment within 5 minutes of initialization, with no additional setup steps.
- **SC-002**: A developer on Windows and a developer on macOS can clone the same Spec-N-Roll project and execute a script-driven workflow step without modifying any configuration files.
- **SC-003**: An interactive specification session produces a complete, checklist-passing specification artifact for a complex feature in 3 or fewer iterative question rounds.
- **SC-004**: After 3 feature implementation cycles, the living specification files accurately describe 100% of delivered feature behaviors with no stale scenarios. Behavioral contradictions are detected by Cucumber test runs.
- **SC-005**: A papercut-tier workflow (`specify → implement`) completes in fewer steps than the full tier (`specify → plan → tasks → implement`).
- **SC-006**: A custom extension step can be registered and used in a workflow run without modifying any file in the toolkit's core directory.
- **SC-007**: All generated Cucumber tests remain passing after an internal refactor that does not change observable application behavior, with zero test modifications required.
- **SC-008**: A developer unfamiliar with Spec-N-Roll can understand the full workflow and the extension mechanism by reading the documentation alone, without consulting source files.
- **SC-009**: A developer can initialize a project, apply a toolkit update, and add a new agent to an existing project using only non-interactive CLI subcommands (`spec-n-roll <subcommand>`), with no manual file editing required.
- **SC-012**: Every deterministic workflow state mutation available to agents via MCP has a matching non-interactive CLI subcommand that invokes the same core-library operation and exits synchronously.
- **SC-010**: After a toolkit update, all user-owned config files and custom extensions remain fully functional with zero manual intervention, provided no breaking schema changes were introduced.
- **SC-011**: A project initialized with an older toolkit version can apply a new version's update without data loss — user-owned files are byte-identical before and after the update (excluding migrations explicitly confirmed by the developer).

---

## Assumptions

- Agents are configured only via extensions; cursor, claude-code, copilot, and codex ship as extensions. The toolkit does not verify that selected agents are installed.
- The project will be used in environments with Git installed; version control integration (branching, commit hooks) is in scope but not required for core workflow functionality.
- Cucumber/Gherkin tooling for test execution is the responsibility of the project under development, not the toolkit itself; the toolkit generates Gherkin files and step scaffolding but does not bundle a test runner.
- Multi-agent support targets agents that read skills from `.agents/` and rules via pointer files to `.spec-n-roll/AGENTS.md`; agents requiring proprietary configuration beyond this model are out of scope for v1.
- Platform auto-detection covers Windows (PowerShell) and Unix-like systems (bash/sh); other shell environments (fish, zsh-specific features) are not required for the initial release.
- The iterative specification session (grill-me style) operates within a single agent conversation session; persistence of session state across multiple conversations is desirable but not required for v1.
- The CLI tool requires a Node.js/TypeScript runtime on the developer's machine; this is treated as a prerequisite for CLI-based setup and update operations.
- The toolkit is designed for use within a project directory; the CLI tool handles both initial bootstrapping and subsequent update/modification operations from within or targeting the project directory.
