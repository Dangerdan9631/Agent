{
  "name": "agentconfig-cli",
  "version": "0.1.0",
  "description": "CLI for agentconfig — generate native AI agent directive files from a unified source",
  "license": "MIT",
  "bin": {
    "agentconfig": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "agentconfig": "*",
    "commander": "^11.0.0",
    "chalk": "^4.1.2",
    "chokidar": "^3.5.3"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.3",
    "@types/node": "^20.0.0"
  }
}
