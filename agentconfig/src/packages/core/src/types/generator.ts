// Re-exported from agentconfig-api — the canonical source of truth for generator / plugin types.
export type {
  FileOutput,
  GeneratorInput,
  AgentGenerator,
  DetectedAgent,
  AgentImportResult,
  AgentTargetPlugin,
  WriteOptions,
  DirectiveTypePlugin,
} from 'agentconfig-api';
