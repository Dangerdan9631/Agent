// Re-exported from agentconfig-api — the canonical source of truth for IR types.
export type {
  IR,
  IRExtensions,
  InstructionFile,
  AgentDefinition,
  SkillDefinition,
  SkillFile,
  CommandDefinition,
  HookDefinition,
  HookType,
  HookEventName,
  ActivationType,
} from 'agentconfig-api';
