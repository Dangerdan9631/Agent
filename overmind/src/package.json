{
  "name": "overmind-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/service && npm run build -w packages/cli && npm run build -w packages/mcp && npm run build -w packages/ui",
    "build:node": "npm run build -w packages/core && npm run build -w packages/service && npm run build -w packages/cli && npm run build -w packages/mcp",
    "test": "vitest run --config vitest.unit.config.ts",
    "test:watch": "vitest --config vitest.unit.config.ts",
    "lint": "eslint packages/",
    "lint:fix": "eslint packages/ --fix",
    "clean": "rimraf packages/*/dist"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "rimraf": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
