{
  "name": "overmind-service",
  "version": "0.1.0",
  "description": "Overmind service process — manages agent state machines.",
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "overmind-service": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "overmind": "*",
    "robot3": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3"
  }
}
