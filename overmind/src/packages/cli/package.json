{
  "name": "overmind-cli",
  "version": "0.1.0",
  "description": "CLI client for the Overmind scaffold service.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "overmind": "dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts src/bin.ts --dts --format cjs --clean",
    "dev": "tsup src/index.ts src/bin.ts --dts --format cjs --watch"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "overmind-api": "0.1.0",
    "overmind-service": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3"
  }
}