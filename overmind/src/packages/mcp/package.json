{
  "name": "overmind-mcp",
  "version": "0.1.0",
  "description": "MCP server for the overmind agent management system.",
  "license": "MIT",
  "main": "dist/index.js",
  "bin": {
    "overmind-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "overmind": "*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3"
  }
}
